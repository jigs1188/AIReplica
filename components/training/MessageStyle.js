import React from 'react';
import { ThemedView } from '../ThemedView';
import { ThemedText } from '../ThemedText';

export default function MessageStyle() {
  return (
    <ThemedView>
      <ThemedText>Message Style</ThemedText>
    </ThemedView>
  );
}
